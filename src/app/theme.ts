/* theme.ts */
import { extendTheme } from "@chakra-ui/react";
import { Colors } from "@/app/foundations/colors";

// export
const theme = extendTheme({
  colors: Colors,
  
});

export default theme;
